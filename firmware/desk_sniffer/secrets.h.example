// Copia este fichero como secrets.h y rellena los valores.
//
// secrets.h NO se versiona: lleva credenciales. El .example si, para que se
// sepa que hace falta.
//
// El broker vive en Ultron (Raspberry Pi 5), contenedor eclipse-mosquitto:2,
// levantado el 2026-08-22. La contrasena tambien esta en Ultron, en
// ~/mosquitto/config/.pass-esp32

#pragma once

#define WIFI_SSID      "..."
#define WIFI_PASSWORD  "..."

#define MQTT_HOST      "192.168.1.29"   // Ultron en la LAN
#define MQTT_PORT      1883
#define MQTT_USER      "esp32"
#define MQTT_PASSWORD  "..."

// Identidad del aparato en Home Assistant. Se usa para los topics y para el
// unique_id de las entidades: cambiarlo crea entidades nuevas y deja las
// viejas huerfanas.
// Contrasena para actualizar el firmware por red (OTA).
#define OTA_PASSWORD   "..."

#define DEVICE_ID      "escritorio_jiecang"
#define DEVICE_NAME    "Escritorio"
